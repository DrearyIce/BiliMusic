<UserControl x:Class="BiliMusic.Controls.Flyout.ChangeVolume"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
             xmlns:local="clr-namespace:BiliMusic.Controls.Flyout"
             mc:Ignorable="d" 
             Height="80"
             Width="290">
    <Grid>
        <Slider x:Name="_Slider" Margin="20,15,20,0" VerticalAlignment="Top" BorderThickness="2" IsSnapToTickEnabled="True" TickFrequency="1" TickPlacement="BottomRight" Maximum="100" Minimum="0"/>
        <TextBlock Text="0" Margin="20,40,0,0" Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
        <TextBlock Text="100" Margin="0,40,20,0" TextAlignment="Right" Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}"/>
    </Grid>
</UserControl>
